# Watchlist API Configuration
# If running letterboxd-list-radarr locally, set this to http://localhost:5000
# Otherwise, leave unset to use the hosted version
WATCHLIST_API_BASE=http://localhost:5000

# Server Configuration
PORT=3000
